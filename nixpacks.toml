[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[variables]
HOST = "0.0.0.0"
PORT = "3000"
NITRO_HOST = "0.0.0.0"
NITRO_PORT = "3000"

[start]
cmd = "npm run start"
